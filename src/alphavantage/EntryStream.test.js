// @flow strict
import assert from "assert"
// import fs from "fs"
// import stream from "stream"
// // import { Entry, CsvToEntry } from "./EntryStream"

describe("EntryStream", () => {
  test("should return lines", (done) => {
    // const rawStream = fs.createReadStream("./src/alphavantage/daily_adjusted_MSFT.test.csv")
    // const entryStream = rawStream.pipe(new CsvToEntry())
    // entryStream.

    // TODO test with chunks of different size QuickCheck style???
    assert.equal(1, 1)
    done()
  })
})
